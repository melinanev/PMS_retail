export interface productData {
        id?: number;
        sku: string; 
        name: string;
        description: string;
        quantity: number;
        price: number;
        category: string; 
        image?: string;
        createdAt?: Date; 
        updatedAt?: Date;
      }

